---
import "@fontsource-variable/outfit/index.css";
import "@fontsource/jetbrains-mono/latin-400.css";
import "@fontsource/jetbrains-mono/latin-500.css";

export interface Props {
  ogImage?: string;
  title?: string;
  description?: string;
  canonicalUrl?: string;
}
const { ogImage, title, description, canonicalUrl } = Astro.props;
const defaultOgImage = ogImage || "/og-image.png";
---
<!doctype html>
<html lang="en">
  <head>
    <!-- Google tag (gtag.js) -->
    <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-JZ37E49EB3"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-JZ37E49EB3');
    </script>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="index, follow" />
    {title ? <title>{title}</title> : null}
    {description ? <meta name="description" content={description} /> : null}
    {canonicalUrl ? <link rel="canonical" href={canonicalUrl} /> : null}
    <meta name="theme-color" content="#059669" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:image" content={`https://www.small-text-gen.com${defaultOgImage}`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:image" content={`https://www.small-text-gen.com${defaultOgImage}`} />
    <slot name="extra-head" />
  </head>
  <body class="min-h-[100dvh] font-sans">
    <slot />
  </body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>